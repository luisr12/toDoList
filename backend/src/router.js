const express = require('express');
const router = express.Router();


//router.get('/tasks', (req, res)=>{res.status(200).send(`O router esta funcionando`)}) terá que retornar um array do meu banco

module.exports=router;